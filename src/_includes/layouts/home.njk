{% extends "layouts/base.njk" %}
{% block content %}
  <div class="article">
    
    <img alt="{{ coverImgAlt }}" src="{{ coverImg }}" class="h-full"/>

    <div class="article__content flow">
      
      <div class="article__header">
        <h2>{{ title }}</h2>
        
        <p>{{ summary }}</p>
      </div>
      
    </div>

          <div class="article__footer" id="articleFooter">
        {% include "partials/author.njk" %}
        {% include "partials/article-share.njk" %}
        <!-- Button is inserted here -->
          </div>
          
  </div>

{% endblock %}
